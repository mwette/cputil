#!/bin/sh
#

if [ ! -f ../Makefile.in-orig ]; then
  cp ../Makefile.in ../Makefile.in-orig
fi

ed ../Makefile.in <<'EndOfInput'
/TOOLS = 
/lackey
a
               cputil  \
.
w
q
EndOfInput

# --- last line ---
